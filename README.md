# website

currently in the stage of learning html (i was going to learn golang first but plans changed for some reason that i dont know either) and i'll write the website when i learn enough to be able to create the page that i want

update at 04:14: im using vim to make these and its my first time actually using vim and i like it so far. also i learned that because i didnt put `<b>` and `</b>` for most of the lines it stacks them on one line, gotta fix that. also when u host the page on anywhere other than `/`, it tries to find `meow.html` in `/`. i need to edit the path as `./meow.html` instead.
